buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.8.0'
        // Certifique-se de usar uma versão compatível
    }
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}